import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import '../providers/matchProvider.dart';

class Settle extends StatelessWidget {
  const Settle({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
